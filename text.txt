3 Bug kritis (akan crash saat runtime):

Bug 1 — `core/brain.py`: `otto_self = self_summary_text()` di-generate tapi tidak dimasukkan ke return. `_build_system_prompt` return tanpa menyertakan `otto_self`. Selain itu, import `from self.model import self_summary_text` akan gagal karena `self` adalah keyword Python — nama folder/module `self` bentrok dengan built-in. Harus diganti misal `from otto_self.model import ...` atau `from . import` dengan struktur package yang benar.

Bug 2 — `server/app.py`: `watcher`, `profiler`, `curiosity`, `scheduler` dideklarasikan lokal di dalam `lifespan()` tapi dipakai di `_handle_text()` dan `_broadcast_curiosity_with_pending()` sebagai global. Harus ditambah `global watcher, profiler, curiosity, scheduler` di dalam `lifespan()`.

Bug 3 — `server/app.py` + `core/memory.py`: `app.py` memanggil `memory.get_temp()`, `memory.set_temp()`, dan `memory.delete_temp()` — tapi tiga method ini tidak ada di `core/memory.py`.

3 Warning (tidak crash tapi perilaku salah):

Warning 4 — `skills/reminder.py`: `VOICE_MODEL` masih pakai path lama `libo-ai`: `/data/asd/libo-ai/voices/...` — harus `/data/asd/otto-ai/voices/...`.

Warning 5 — `server/app.py` `_load_skills()`: Memanggil `mod.register(ex)` tapi skill modules mengekspos fungsi bernama `register_system_skills()`, `register_media_skills()`, `register_reminder_skills()`. Nama tidak match, skills tidak akan pernah terdaftar.

Warning 6 — `core/brain.py`: `otto_self` digenerate tapi tidak dipakai — `_BASE_SYSTEM` tidak punya placeholder `{self_section}` sehingga self-awareness Otto tidak pernah masuk ke system prompt.



beri tahu aku fix nya dengan contoh sebelum dan sesudah
